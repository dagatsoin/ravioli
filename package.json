{
  "name": "warfog.ravioli",
  "version": "1.3.0-beta.0",
  "description": "State management system based on State Action Model pattern.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^27.5.1",
    "jest": "^28.1.0",
    "mobx": "^6.6.0",
    "ts-jest": "^28.0.3",
    "typescript": "^4.7.2"
  },
  "peerDependencies": {
    "mobx": "^6.6.0"
  }
}
